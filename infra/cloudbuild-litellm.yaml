steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - -f
      - litellm/Dockerfile
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: us-central1-docker.pkg.dev/your-gcp-project/buzz/litellm:latest
options:
  logging: CLOUD_LOGGING_ONLY
